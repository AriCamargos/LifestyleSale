import 'package:flutter/material.dart';

class TagFavorite extends StatelessWidget {
  const TagFavorite({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}